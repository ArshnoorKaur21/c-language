//program 
#include<stdio.h>
main()
{
	int loc=-1;
	int i,n,item;
	printf("enter size of array");
	scanf("%d",&n);
	int
	
	printf("linear searching");
	for(i=0;i<n;i++)
	{
		if(a[i]==item)
		{
			loc=i;
			printf("element found at the location";)
		}
	}
	if(i==n)
	printf("elemetn not found at location");
	return 0;
}
