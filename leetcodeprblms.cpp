//program of median of two sorted arrays
#include<stdio.h>
main()
{
   int a[5];
   int b[5];
   printf("\nfirst array");
   for(i=0;i<5;i++)
   {
   	scanf("%d",&a[i]);
   }
   printf("\nsecond array");
   for(i=0;i<5;i++)
   {
   	scanf("%d",&b[i]);
   }
   printf("\nmerged array of 1sta nd second");
   
   
}
